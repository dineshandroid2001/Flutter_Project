name: chat_box
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable:
  flutter_bloc:
  bloc:
  firebase_auth:
  cloud_firestore:
  firebase_core:
  cupertino_icons: ^1.0.2
  contacts_service:
  permission_handler:
  image_picker:
  snackbar:
  url_launcher:
  call_log:
  intl:
  sqflite:
  path_provider:
  path:
  lottie:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

